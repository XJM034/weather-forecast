# Spotify 风格天气面板

这个仓库包含一个静态的成都锦江区天气展示页面，采用 Spotify 灵感的深色渐变与玻璃拟态布局。

## 本地预览

使用仓库根目录下的 `preview.sh` 启动一个简单的 HTTP 服务器并访问页面：

```bash
./preview.sh [端口:可选 默认为 8000] [绑定地址:可选 默认为 0.0.0.0]
```

启动后访问脚本输出的 URL（默认 http://localhost:8000/index.html）即可查看页面。
